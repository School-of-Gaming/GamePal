# GamePal
GamePal is a website where parents and children can match and find their penpal safely.


## Running the Frontend Server
`cd ..`  # Navigate to the frontend directory (GamePal) 
`npm install`    # Install dependencies  
`npm run dev`    # Start the frontend server